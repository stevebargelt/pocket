{
  "context": "code",
  "source": "Express (curated TypeScript seed)",
  "license": "MIT",
  "note": "Hand-curated TypeScript lines representative of an Express-style HTTP service (Express is MIT-licensed). Each line is a self-contained statement within the 120-char code cap; overly long imports are left out rather than shipped. Shares the 'code' context with code-python.json and code-bash.json, each carrying its own source. Regenerate from a local OSS checkout with scripts/curate-code.ts. See CREDITS.md.",
  "items": [
    "import express, { Request, Response } from \"express\";",
    "const app = express();",
    "app.use(express.json());",
    "app.get(\"/health\", (req: Request, res: Response) => res.sendStatus(200));",
    "const port = Number(process.env.PORT) || 3000;",
    "app.listen(port, () => console.log(`listening on ${port}`));",
    "export type User = { id: number; name: string; email: string };",
    "interface Repository<T> { findById(id: number): Promise<T | null>; }",
    "const users = new Map<number, User>();",
    "const router = express.Router();",
    "router.use((req, res, next) => { console.log(req.method, req.url); next(); });",
    "app.use(\"/api\", router);",
    "const isValid = (email: string): boolean => /.+@.+\\..+/.test(email);",
    "export const config = { retries: 3, timeout: 5000 } as const;",
    "const result = items.filter((x) => x.active).map((x) => x.id);",
    "const total = orders.reduce((sum, o) => sum + o.amount, 0);",
    "const [first, ...rest] = await Promise.all(tasks);",
    "try { await save(record); } catch (err) { console.error(err); }",
    "type Result<T> = { ok: true; value: T } | { ok: false; error: string };",
    "const cache = new WeakMap<object, string>();",
    "async function fetchJson<T>(url: string): Promise<T> { return (await fetch(url)).json(); }",
    "const sorted = [...list].sort((a, b) => a.score - b.score);",
    "enum Status { Pending, Active, Closed }",
    "const now = new Date().toISOString();",
    "const env = process.env.NODE_ENV ?? \"development\";",
    "if (!user) throw new Error(\"user is required\");",
    "const ids = new Set(rows.map((r) => r.id));",
    "const merged = { ...defaults, ...overrides };",
    "const add = (a: number, b: number): number => a + b;",
    "class Service { constructor(private readonly db: Db) {} }",
    "const value = obj?.nested?.field ?? fallback;",
    "function notFound(res: Response) { res.status(404).end(); }",
    "const handler: RequestHandler = (_req, res) => res.send(\"ok\");",
    "const elapsed = Date.now() - start;",
    "res.set(\"Cache-Control\", \"no-store\");"
  ]
}
