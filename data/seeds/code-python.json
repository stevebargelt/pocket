{
  "context": "code",
  "source": "FastAPI (curated Python seed)",
  "license": "MIT",
  "note": "Hand-curated Python lines representative of a FastAPI-style service (FastAPI is MIT-licensed). Self-contained statements within the 120-char code cap; indentation is preserved to make realistic typing practice. Shares the 'code' context with code-typescript.json and code-bash.json. Regenerate from a local OSS checkout with scripts/curate-code.ts. See CREDITS.md.",
  "items": [
    "from fastapi import FastAPI, HTTPException",
    "from pydantic import BaseModel",
    "app = FastAPI(title=\"users-api\")",
    "@app.get(\"/health\")",
    "def health() -> dict[str, str]:",
    "    return {\"status\": \"ok\"}",
    "class User(BaseModel):",
    "    id: int = 0",
    "    name: str = \"\"",
    "    email: str = \"\"",
    "def factorial(n: int) -> int:",
    "    return 1 if n <= 1 else n * factorial(n - 1)",
    "items = [x for x in range(100) if x % 2 == 0]",
    "total = sum(o.amount for o in orders)",
    "names = sorted(users, key=lambda u: u.name)",
    "result = {k: v for k, v in pairs if v is not None}",
    "rows = [line.strip().split(\",\") for line in lines]",
    "unique_ids = {row.id for row in rows}",
    "merged = {**defaults, **overrides}",
    "config = {\"retries\": 3, \"timeout\": 5.0}",
    "db: dict[int, User] = {}",
    "first, *rest = await asyncio.gather(*tasks)",
    "logger = logging.getLogger(__name__)",
    "env = os.environ.get(\"NODE_ENV\", \"development\")",
    "@app.post(\"/users\", status_code=201)",
    "async def create_user(user: User) -> User:",
    "    db[user.id] = user",
    "    return user",
    "async def fetch(url: str) -> dict:",
    "    async with session.get(url) as resp:",
    "        return await resp.json()",
    "if not user:",
    "    raise HTTPException(status_code=404, detail=\"not found\")",
    "def __repr__(self) -> str:",
    "    return f\"User(id={self.id}, name={self.name!r})\"",
    "value = data.get(\"key\", default)",
    "squares = {n: n * n for n in range(1, 11)}",
    "assert response.status_code == 200"
  ]
}
